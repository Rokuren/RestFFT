#pragma once
#ifndef SERVER_H
#define SERVER_H
#include <cpprest/http_listener.h>
#include <iostream>
#include <complex>
#include <string.h>
#include <chrono>
#include <thread>
#include "lodepng.h"
/*
void handle_get(web::http::http_request);
void handle_post(web::http::http_request);
float pack_color(unsigned char rgba[4]);

struct ImageArray {
	int32_t number_of_dimensions;
	int32_t dimensions[100]; // width ?
	std::complex<float> data[120]; // height ?

	ImageArray() {
		number_of_dimensions = 2;
	}
};

struct Date {
	int month;
	int day;
	int year;

	Date() {
		month = 1;
		day = 5;
		year = 1989;
	}
};
*/
#endif